# Demo evidence

Place your **screenshots** (or link to a short video) here for hackathon submission.

Suggested files (see `docs/DEMO_EVIDENCE_STEPS.md` for full steps):

- `01_api_docs.png` – Swagger/API docs page
- `02_tools_list.png` – Response from `GET /api/tools/list`
- `03_demand_forecast.png` – Response from `GET /api/demand_forecast`
- `04_combo_recommendations.png` – Response from `GET /api/combo_recommendations`
- `05_staffing_recommendation.png` – Response from `GET /api/staffing_recommendation`

If using OpenClaw: screenshots of OpenClaw invoking these endpoints (e.g. `openclaw_demand.png`, `openclaw_combos.png`).
